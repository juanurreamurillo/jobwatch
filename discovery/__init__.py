"""Phase 0 endpoint-discovery tooling for jobwatch.

Not part of the runtime package — a dev aid to characterize each portal's
search page (SSR vs SPA, Cloudflare, JSON-LD, listing selectors) before
writing its connector. See docs/endpoints.md.
"""
